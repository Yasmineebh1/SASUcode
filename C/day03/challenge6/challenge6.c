#include <stdio.h>
#include <string.h>

void strcount()
{
    int i;
    char s[50];

    int found = 0;
    for (i = 0; c[i] != '\0'; i++)
    {
            if (c[i] == m)
            {
                found = 1;
                printf("present%d", m);
            }
        }
    }

int main()
{
    char c[50];
    char c2[50];
    int n;

    puts("entrer un chaine de caractere: ");
    gets(c);
    puts("entrer un chaine de caractere: ");
    gets(c2);

    puts(strchr(c, "y"));

    // x = puts(strlen(c));
}
#include <stdio.h>
#include <string.h>

int main()
{
    char c[100];

    printf("entrer une charactere: ");
    gets(c);
    puts(c);

    return 0;

    // printf("la chaine de caractere que vous creez est: ",c);
}
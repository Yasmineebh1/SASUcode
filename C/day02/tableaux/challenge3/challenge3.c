#include<stdio.h>

int main(){
    int t[100],i,m,n,s;

    // printf("entrer les nombres elements des tableaux: ");
    // scanf("%d",&n);

    printf("entrer le 1er nombre: ");
        scanf("%d",&n);
        printf("entrer le 2eme nombre: ");
        scanf("%d",&m);
    for(i = 0;i < t[i]; i++){
        // printf("%d",&t[i]);
        // printf("la somme est %d + %d = %d",n,m);
        s = m + n;}
        printf("la somme est %d", s);
       // printf("%d",t[i]);

        // for(i = 0; i < n; i++){
            
            
        // }
    
}